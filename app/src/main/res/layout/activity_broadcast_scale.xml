<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.qingniu.qnble.demo.view.BroadcastScaleActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="备注：(0 为 kg，默认值 1 为 lb,磅，所有秤都能够支持这个单位 2 为 斤，秤端如果不支持，则会显示kg 3 为 st，英石，秤端如果不支持，则会显示lb，的数值)"
        android:textSize="12sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <EditText
            android:id="@+id/unit_edit"
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:hint="输入要切换的单位"
            android:inputType="number"
            android:textSize="10sp" />

        <Button
            android:id="@+id/setUnit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="设置秤单位" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="此功能只双模称有效" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:layout_width="wrap_content"
            android:text="收到实时重量："
            android:layout_height="wrap_content" />
        <TextView
            android:id="@+id/weightTv"
            android:layout_width="wrap_content"
            android:textColor="@android:color/holo_red_dark"
            android:layout_height="wrap_content" />
    </LinearLayout>
    <ListView
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />
</LinearLayout>
